# 司法院裁判書自動爬蟲

自動從[司法院裁判書查詢系統](https://judgment.judicial.gov.tw/FJUD/default.aspx)爬取裁判書，解析結構化資料並匯出 Excel。

## 功能

- 關鍵字搜尋，依裁判日期遞迴細分區間，突破單一查詢 500 筆上限
- 裁判日期為伺服器端篩選，精確到日
- 裁判法院、案件類別（民事／刑事／行政／憲法／懲戒）為伺服器端篩選，可不給關鍵字純條件查詢
- 裁判種類（判決／裁定）於結果清單頁過濾，在下載前就濾掉，大幅減少請求數
- 案號年度為伺服器端分群篩選，與裁判日期是各自獨立的維度，可任意組合
- 只要判決（或裁定）時自動以全文檢索做伺服器端粗篩，結果集大幅縮小、翻頁量減少
- 逐月長時間爬取可中斷後接續，全部完成後匯出成一份依裁判日期排序的 Excel
- 速率控制：隨機抖動、遇錯自動降速、連續失敗長暫停，並自動復原伺服器的「查詢設定錯誤」頁
- 解析 20+ 個欄位：裁判字號、法院、當事人、主文、事實及理由、法官等
- 支援所有法院類型，包含憲法法庭特殊格式
- 匯出格式化 Excel，含法院分布統計摘要

進階查詢頁各欄位的支援情況與目前的限制，請見文末
[進階查詢欄位支援對照](#進階查詢欄位支援對照) 與 [已知限制](#已知限制)。

## 環境需求

- Python 3.9+
- Google Chrome（版本需與 chromedriver 相符，`webdriver-manager` 會自動管理）

## 安裝

```bash
python -m venv .venv
.venv\Scripts\activate        # Windows
# source .venv/bin/activate   # macOS / Linux

pip install -r requirements.txt
```

## 使用方式

### 一鍵完整流程（推薦）

```bash
python pipeline.py [keyword] -n <筆數> [選項]
```

| 參數 | 說明 |
|------|------|
| `[keyword]` | 搜尋關鍵字，例如 `借名登記`、`詐欺`；指定 `--court` 或 `--case-type` 時可省略 |
| `-n <筆數>` | 目標爬取筆數，例如 `-n 1000`；`0` = 不設上限，把範圍內全部爬完 |
| `--court <法院>` | 裁判法院，名稱或代碼（如 `臺灣臺北地方法院`、`台北地院`、`TPD`），伺服器端篩選 |
| `--case-type <類別>` | 案件類別，逗號分隔（`憲法`/`民事`/`刑事`/`行政`/`懲戒`），伺服器端篩選 |
| `--judgment-type <種類>` | 裁判種類（`判決` 或 `裁定`），於結果清單頁過濾 |
| `--delay <秒>` | 請求基礎間隔秒數，預設 1.0；遇錯自動降速、連續失敗長暫停（長時間爬取建議 1.5~2） |
| `--start-year <年>` | 搜尋起始年（西元），展開為該年 1/1，預設 2015 |
| `--end-year <年>` | 搜尋結束年（西元），展開為該年 12/31；**不指定時迄日是「今天」而非當年 12/31** |
| `--start-date <日期>` | 裁判日期起，格式 `YYYY/MM/DD`（西元），會覆蓋 `--start-year` |
| `--end-date <日期>` | 裁判日期迄，格式 `YYYY/MM/DD`（西元），會覆蓋 `--end-year` |
| `--case-year-start <年>` | 案號年度起（民國年，如 `113`），伺服器端分群 |
| `--case-year-end <年>` | 案號年度迄（民國年，如 `115`），伺服器端分群 |
| `--no-headless` | 顯示瀏覽器視窗，方便除錯觀察 |
| `-o <檔名>` | 指定輸出 Excel 檔名，預設自動加時間戳 |
| `--full-text` | Excel 含完整全文欄位（檔案較大） |
| `--skip-crawl` | 跳過爬取，只重新解析 + 匯出（更新 parser 後使用） |

依序執行爬取 → 解析 → 匯出 Excel，完成後在當前目錄產生 `judgments_借名登記_YYYYMMDD_HHMMSS.xlsx`。

> **無關鍵字查詢時的資料標籤**：只給 `--court`／`--case-type` 而不給關鍵字時，
> DB 的 `keyword` 欄會寫入自動組出的標籤（例：`臺灣臺北地方法院-民事-判決`），
> Excel 檔名與 `export_excel.py -k` 都以它為準，用來區分不同批次的資料。
>
> **匯出是累積的**：第 3 步匯出的是資料庫中「該標籤的全部資料」，不限本次爬取的日期，
> 所以分次跑 1 月、2 月後，第二次產生的 Excel 會同時包含 1、2 月；排序依解析時間由新到舊。
> 需要依裁判日期排序的單一檔案，請用 `crawl_monthly.py --export-only`。

**範例：**

```bash
# 基本用法：搜尋「借名登記」，目標 1000 筆
python pipeline.py 借名登記 -n 1000

# 限制年份範圍
python pipeline.py 借名登記 -n 1000 --start-year 2020 --end-year 2024

# 精確日期範圍
python pipeline.py 借名登記 -n 500 --start-date 2024/01/01 --end-date 2024/12/31

# 顯示瀏覽器視窗（除錯用）
python pipeline.py 借名登記 -n 100 --no-headless

# 裁判日期 + 案號年度（兩個獨立維度，可組合）
python pipeline.py 借名登記 -n 500 --start-date 2024/01/01 --end-date 2024/12/31 --case-year-start 113 --case-year-end 113

# 不給關鍵字，純條件查詢：民國 114 年（裁判日期）臺北地院所有民事判決
python pipeline.py -n 0 --court 臺灣臺北地方法院 --case-type 民事 --judgment-type 判決 \
       --start-date 2025/01/01 --end-date 2025/12/31

# 跳過爬取，只重新解析 + 匯出
python pipeline.py 借名登記 --skip-crawl
```

---

### 逐月長時間爬取（crawl_monthly.py）

整年這類要跑好幾個小時的任務，用這支腳本以「月」為單位執行，可中斷後接續，
全部完成後才匯出成**一份** Excel。預設條件為 2025 年（民國 114 年）3~12 月
臺灣臺北地方法院民事**判決**（裁定在清單頁就濾掉，不會下載）。

```bash
python crawl_monthly.py --dry-run     # 先預覽：各月狀態、DB 現有筆數、預估時間（不連網）
python crawl_monthly.py               # 開始跑；中斷後重新執行同一個指令即可接續
```

| 參數 | 說明 |
|------|------|
| `--year <年>` | 西元年，預設 2025 |
| `--months <月份>` | 要**爬取**的月份，如 `3-12` 或 `3,5,7-9`，預設 `3-12`；匯出預設也是這些月份 |
| `--court` / `--case-type` / `--judgment-type` | 查詢條件，預設 `臺灣臺北地方法院` / `民事` / `判決` |
| `--delay <秒>` | 請求基礎間隔，預設 1.5；遇錯自動降速，且整個執行共用降速狀態 |
| `--state <路徑>` | 進度狀態檔，預設依條件與年份自動命名（`crawl_state_*.json`） |
| `--force` | 忽略狀態檔，已完成的月份也重跑 |
| `--dry-run` | 只顯示計畫與目前進度，不連網 |
| `--export-only` | 不爬取，直接解析並匯出 |
| `--no-export` | 只爬取，不匯出 |
| `--export-all` | 匯出擴大為該年 1~12 月（**爬取範圍不變，仍由 `--months` 決定**） |
| `-o <檔名>` / `--full-text` | 輸出檔名、是否含全文欄位 |
| `--allow-sleep` | 不阻止系統睡眠（預設執行期間保持喚醒；Windows／macOS／Linux 皆支援） |
| `--no-headless` | 顯示瀏覽器視窗 |

**接續機制**：每個月跑完就寫入狀態檔。重新執行時，狀態為 `done` 的月份直接跳過；
有「清單頁錯誤未復原」的月份標為 `incomplete`、被中斷的標為 `interrupted`、
發生例外的標為 `error`，這些都會在下次執行時重試。重跑同一個月時，已下載的裁判書
會自動跳過，只有清單頁需要重翻。連續兩個月發生例外（多半是斷網）會自動停止，避免空轉。

**匯出依條件而非標籤**：以「法院＋案件類別＋裁判種類＋月份」撈資料並依裁判日期排序，
所以先前被其他批次（例如關鍵字「借名登記」）抓過的同條件判決也會納入。

反過來說，`--export-all` 不會去補爬：要求的月份若資料庫裡沒有，匯出時會逐月標出
「⚠ 沒有資料」且不中斷（全部皆空才不產生檔案）。**檔名依實際有資料的月份命名**——
只有 3~12 月有資料時是 `..._2025_03-12_....xlsx` 而非「全年」，不連續的月份以 `+`
串接（`01+03-04+12`）。

**保持喚醒**：執行期間會要求系統不要閒置睡眠，程式結束（含被中斷）即自動恢復，
不修改任何持久設定。三個平台各用各的機制：

| 系統 | 機制 |
|---|---|
| Windows | `SetThreadExecutionState(ES_CONTINUOUS \| ES_SYSTEM_REQUIRED)` |
| macOS | `caffeinate -i -w <pid>` 子行程（主程序被強制結束也會自行收掉） |
| Linux | `systemd-inhibit --what=idle:sleep --mode=block` |

都只擋「系統睡眠」、不擋螢幕關閉。找不到對應機制時（例如沒有 systemd）會安靜降級，
不影響爬取，但**開頭那行「已要求系統在執行期間保持喚醒」就不會出現**——沒看到它就代表
這台機器不會被擋睡，請自行調整電源設定。闔上筆電蓋子仍可能依系統設定而睡眠，三個平台皆然。

```bash
python crawl_monthly.py --months 8-12                 # 只跑 8~12 月
python crawl_monthly.py --export-only --export-all    # 不爬，匯出全年一份 Excel
```

---

### 分步執行

若需要個別控制每個步驟：

**步驟 1：爬取**

```bash
python crawl_batched.py [keyword] -n <筆數> [選項]
```

| 參數 | 說明 |
|------|------|
| `[keyword]` | 搜尋關鍵字（指定 `--court` 或 `--case-type` 時可省略） |
| `-n <筆數>` | 目標爬取筆數；`0` = 不設上限 |
| `--court <法院>` | 裁判法院（名稱或代碼），伺服器端篩選 |
| `--case-type <類別>` | 案件類別，逗號分隔（憲法/民事/刑事/行政/懲戒），伺服器端篩選 |
| `--judgment-type <種類>` | 裁判種類（判決／裁定），結果清單頁過濾 |
| `--delay <秒>` | 請求基礎間隔秒數，預設 1.0；遇錯自動降速、連續失敗長暫停（長時間爬取建議 1.5~2） |
| `--start-year <年>` | 搜尋起始年（西元），展開為該年 1/1，預設 2015 |
| `--end-year <年>` | 搜尋結束年（西元），展開為該年 12/31；不指定時迄日是今天 |
| `--start-date <日期>` | 裁判日期起 `YYYY/MM/DD`（西元，伺服器端精確篩選） |
| `--end-date <日期>` | 裁判日期迄 `YYYY/MM/DD`（西元，伺服器端精確篩選） |
| `--case-year-start <年>` | 案號年度起（民國年，如 `113`），伺服器端分群 |
| `--case-year-end <年>` | 案號年度迄（民國年，如 `115`），伺服器端分群 |
| `--no-headless` | 顯示瀏覽器視窗 |

```bash
python crawl_batched.py 借名登記 -n 1000
python crawl_batched.py 借名登記 -n 1000 --start-year 2018 --end-year 2023
python crawl_batched.py -n 0 --court 臺灣臺北地方法院 --case-type 民事 --judgment-type 判決 \
       --start-date 2025/01/01 --end-date 2025/12/31
```

HTML 快取存於 `html_cache/`，爬取紀錄寫入 `judgments.db`。

**步驟 2：解析**

```bash
python html_parser.py [選項]
```

| 參數 | 說明 |
|------|------|
| （無參數） | 解析所有尚未解析的新紀錄 |
| `--reparse` | 清空已解析資料，重新解析全部（更新 parser 邏輯後使用） |
| `--file <路徑>` | 僅解析指定的單一 HTML 檔案（測試用） |

```bash
python html_parser.py            # 解析新增的紀錄
python html_parser.py --reparse  # 全部重新解析
```

**步驟 3：匯出 Excel**

```bash
python export_excel.py [選項]
```

| 參數 | 說明 |
|------|------|
| `-n <筆數>` | 匯出筆數，`0` 表示全部，預設 100 |
| `-k <關鍵字>` | 篩選關鍵字（比對搜尋關鍵字、裁判字號、主文） |
| `-c <法院>` | 篩選法院（部分比對，例如 `臺灣臺北`） |
| `--start-date <日期>` | 篩選裁判日期起（`YYYY/MM/DD` 或民國格式皆可） |
| `--end-date <日期>` | 篩選裁判日期迄（`YYYY/MM/DD` 或民國格式皆可） |
| `--offset <N>` | 略過前 N 筆 |
| `--full-text` | 含完整全文欄位（檔案較大） |
| `-o <檔名>` | 指定輸出檔名 |

```bash
python export_excel.py -n 0 -k 借名登記       # 匯出全部借名登記資料
python export_excel.py -n 100                  # 匯出最新解析的 100 筆
python export_excel.py -n 0 -c 臺灣臺北       # 篩選臺北相關法院
python export_excel.py -n 0 --full-text        # 含全文欄位
```

排序固定為**解析時間由新到舊**（所以 `-n 100` 是「最近解析的 100 筆」），不是裁判日期。
`-k` 為部分比對，同時比對標籤、裁判字號與主文。Excel 單一儲存格上限約 32,767 字，
超過 32,700 字的欄位會截斷並標註「…(截斷)」，完整內容以資料庫為準。

Excel 的「適用法條」欄是 `applicable_laws_json` 的易讀呈現
（`民法§767第1項前段；民法§470第2項`），與「主要法規」「法條引用數」
「適用法條（結構化JSON）」相鄰，人看易讀欄、程式讀 JSON 欄，兩者同源不會漂移。
資料庫的 `applicable_laws` 原始欄位保留但不匯出——它以正則從「據上論斷」擷取，
覆蓋率僅 34.8% 且其中 85.7% 被截斷（「第436條之1第3項」會停在「第3」）。

---

### 單次爬取（crawler.py）

不做日期切分，直接對單一條件查詢一次。支援法院、案件類別、裁判種類與案號年度篩選：

```bash
python crawler.py [keyword] -n <筆數> [選項]
```

| 參數 | 說明 |
|------|------|
| `[keyword]` | 搜尋關鍵字（指定 `--court` 或 `--case-type` 時可省略） |
| `-n <筆數>` | 爬取筆數，預設 10 |
| `--court <法院>` | 裁判法院（名稱或代碼），伺服器端篩選 |
| `--case-type <類別>` | 案件類別，逗號分隔（憲法/民事/刑事/行政/懲戒），伺服器端篩選 |
| `--judgment-type <種類>` | 裁判種類（判決／裁定），結果清單頁過濾 |
| `--label <標籤>` | 寫入 DB `keyword` 欄的標籤（無關鍵字查詢時用來標記這批資料） |
| `--delay <秒>` | 請求基礎間隔秒數，預設 1.0；遇錯自動降速、連續失敗長暫停（長時間爬取建議 1.5~2） |
| `--start-date <日期>` | 裁判日期起 `YYYY/MM/DD`（西元，伺服器端精確篩選） |
| `--end-date <日期>` | 裁判日期迄 `YYYY/MM/DD`（西元，伺服器端精確篩選） |
| `--case-year-start <年>` | 案號年度起（民國年，如 `113`），伺服器端分群 |
| `--case-year-end <年>` | 案號年度迄（民國年，如 `115`），伺服器端分群 |
| `--no-headless` | 顯示瀏覽器視窗 |
| `--recrawl-stubs` | 重爬資料庫中不完整的 HTML（不需關鍵字） |

```bash
# 指定裁判日期區間
python crawler.py 借名登記 -n 20 --start-date 2024/03/01 --end-date 2024/03/07

# 裁判日期 + 案號年度（兩個獨立維度）
python crawler.py 借名登記 -n 20 --start-date 2024/01/01 --end-date 2024/02/14 --case-year-start 113 --case-year-end 113

# 不給關鍵字的條件查詢（單日）
python crawler.py -n 20 --court 臺灣臺北地方法院 --case-type 民事 --judgment-type 判決 \
       --start-date 2025/01/08 --end-date 2025/01/08 --label TPD民事判決
```

> **`crawler.py` 只送一次查詢，不做日期切分。** 若結果超過單一結果集上限（500 筆），
> 它會改用結果頁分群逐桶收集：未指定 `--court` 時用法院分群，已指定 `--court` 時
> 法院分群只會有一桶、改用案號年度分群。仍能突破 500，但涵蓋上限是「各桶取前 500 筆」
> 之總和；某一桶在此條件下若超過 500 筆，其較舊的部分取不到。
>
> 需要**保證完整**請用 `crawl_batched.py` 或 `pipeline.py`：它們會遞迴細分裁判日期，
> 把每段壓到 500 筆以下，不受此限制。

---

### 補爬不完整的頁面

若某些裁判書頁面因網路問題只儲存到不完整的 HTML，可補爬：

```bash
python crawler.py --recrawl-stubs
```

## 專案結構

```
.
├── pipeline.py          # 完整流程：爬取 → 解析 → 匯出
├── crawl_batched.py     # 分批爬取，依裁判日期遞迴細分突破 500 筆上限
├── crawl_monthly.py     # 逐月長時間爬取（可中斷接續），完成後匯出成一份 Excel
├── crawler.py           # 核心爬蟲（Selenium）
├── html_parser.py       # HTML 解析，結構化存入 SQLite
├── structuring.py       # 民事判決結構化欄位推導規則（純函式，可單元測試）
├── backfill_structured.py # 對既有資料庫回填結構化欄位（不重新解析 HTML）
├── export_excel.py      # 匯出 Excel
├── requirements.txt
│
├── judge_analysis/      # 法官資料分析（獨立於爬蟲，見其 README）
│   ├── run_analysis.py
│   ├── src/             # loader.py（資料攤平）、metrics.py（指標與檢定）
│   └── output/          # 產出（不入版控）
│
├── tests/               # 離線測試（unittest）＋ 線上煙霧測試
│   ├── fixtures/        # 測試用的真實裁判書 HTML 樣本
│   └── live_check.py    # 需連網，手動執行
├── .github/workflows/   # GitHub Actions：push / PR 時跑離線測試
│
├── html_cache/          # 爬取的 HTML 快取（自動建立，不入版控）
└── judgments.db         # SQLite 資料庫（自動建立，不入版控）
```

## 測試

```bash
# 離線測試（不連網、不開瀏覽器，約 3 秒）
python -m unittest discover -s tests -t . -v

# 線上端到端煙霧測試（會開 Chrome 連司法院網站，約 2-4 分鐘）
python tests/live_check.py
python tests/live_check.py --quick     # 只驗查詢條件，不下載裁判書
```

離線測試涵蓋查詢條件轉換（法院／案件類別／日期）、日期切分策略、全文粗篩與分群桶截斷偵測、
URL 欄位解析、HTML 解析（以 `tests/fixtures/` 的真實裁判書為樣本）、清單頁錯誤頁復原、
WebDriver session 重建、速率控制、逐月爬取的中斷接續與條件式匯出、匯出篩選與 Excel 產出，
以及 README 與實際 CLI 參數、預設值的一致性（新增參數卻沒寫進 README 會直接測試失敗）。
資料庫操作一律在暫存 DB 進行，不會動到 `judgments.db`。

線上測試驗證的是「司法院網站是否改版」—— 查詢表單欄位、結果清單版型、分群連結。
它**不**放進 GitHub Actions：對方有 bot-defense，雲端 IP 會被擋而變成假失敗，
也不該讓 CI 定期去打政府機關的伺服器。改完查詢相關的程式碼後請在本機跑一次。

## 運作原理

### 爬取流程

```
crawl_monthly.py（選用）
  │  逐月呼叫 crawl_batched，進度寫入狀態檔；全部完成後解析並依條件匯出一份 Excel
  │
crawl_batched.py
  │  初始以公曆年分段（最新年在前），再依實際筆數遞迴細分
  │  某段 >= 500 筆 → 對半切分，較新的半段先處理（嚴格由新到舊）
  │
  └── crawler.py（每段呼叫一次，共用同一個 WebDriver；
      │            每下載 80 筆會重建 session，新的 driver 隨結果回傳給上層接手）
        │  default_AD.aspx 進階搜尋：關鍵字 + 裁判日期起迄 + 法院 + 案件類別
        │  → 產生該條件組合專屬的 q= hash，各自享有獨立的 500 額度
        │
        ├── 讀摘要頁「查詢結果 N」判斷是否會被截斷
        │     N >= 500 → 回報 truncated，交由上層切分
        │     N <  500 → 翻頁完整收集
        │
        ├── 案號年度（若有指定）→ 伺服器端年度分群 gy=jyear&gc=<年>
        │     只翻符合年度的桶；「N 年以前」統括桶再由本地過濾切邊界
        │
        ├── 裁判種類（若有指定）→ 清單頁依裁判字號尾端的「民事判決／民事裁定」過濾，
        │     在 Phase B 下載之前就濾掉（進階搜尋沒有判決／裁定欄位）
        │     未給關鍵字時，另以「判決」／「裁定」做全文檢索當伺服器端粗篩：
        │     判決書本文必含「判決」，大量本票類裁定則不含（實測單日 976 → 55 筆、漏抓 0），
        │     結果集縮小後幾乎不會撞到 500 上限，翻頁量也大幅減少
        │
        ├── 清單頁若變成「查詢設定錯誤」頁（伺服器端 q= 結果集失效）
        │     → 不視為翻完：重載該頁 → 仍失敗則重送查詢換新 hash → 跳回原頁續翻
        │     → 三次都救不回才計入 page_errors，並由批次層列出需重跑的區段
        │
        └── gy/gc 一次只能用一條軸：若已因單日超限啟用法院分群，
              該區段的案號年度改由本地過濾（正確性不變）。
              已指定 --court 時法院分群只會有一桶，單日超限改用案號年度分群細分。
              若某個分群桶本身仍 >= 500 列（以翻到的原始列數判斷，而非過濾後筆數），
              該桶較舊的部分取不到，會記入 truncated_buckets 並在批次結束時列出
```

### 資料庫結構

| 資料表 | 內容 |
|--------|------|
| `crawl_records` | 爬取紀錄（URL、HTML 路徑、是否已解析） |
| `judgments` | 解析結果（原始欄位 30 欄＋結構化欄位 35 欄） |

### 支援的解析欄位

裁判字號、裁判書連結、法院、裁判日期、案件類型、裁判種類、當事人角色、原告／聲請人、被告／相對人、上訴人、被上訴人（及各方代理人）、主文、事實、事實及理由、犯罪事實、理由、結論、適用法條、法官、書記官、搜尋關鍵字、解析時間

裁判日期一律正規化為西元 ISO（`YYYY-MM-DD`），可直接排序與比較。

### 民事判決結構化欄位

`structuring.py` 在解析當下從既有文字欄位推導出 35 個結構化欄位，涵蓋
案件分類、訴訟結果（本訴／反訴分離）、判准與請求金額、結構化法條引用、
法官與角色、當事人與程序特徵。完整欄位字典見
[judge_analysis/README.md](judge_analysis/README.md)。

三個設計重點：

1. **純函式模組**。`structuring.py` 不碰資料庫、不碰 HTML，同一份輸入永遠
   得到同一份輸出，因此可完整重現、可單元測試（`tests/test_structuring.py`，
   83 個測試，其中每個回歸測試都對應一個實際發生過的缺陷）。
2. **爬取與回填共用同一份規則**。`html_parser.parse_html()` 在解析當下呼叫，
   `backfill_structured.py` 對既有資料回填，兩條路徑不會產生不一致。
3. **可稽核**。推導值一律附帶來源標記（`claimed_source`）或品質旗標
   （`quality_flags`），下游分析知道哪些是直接抽取、哪些是推論、哪些要複查。

```bash
# 規則改版後回填（會自動備份資料庫）
python backfill_structured.py --court 臺北 --year 2025
python backfill_structured.py --dry-run          # 只看分布，不寫入
python backfill_structured.py --only-missing     # 只刷新規則版本較舊的列
python backfill_structured.py --no-backup        # 略過資料庫備份（不建議）
```

### 法官資料分析

回填完成後即可產生法官層級的報表：

```bash
python judge_analysis/run_analysis.py
python judge_analysis/run_analysis.py --min-cases 30   # 納入統計檢定的最低案件數
```

方法論與完整欄位字典見 [judge_analysis/README.md](judge_analysis/README.md)。
重點是**不能直接比較法官的原始勝訴率**：案件組合不同會把分派差異誤讀成
心證傾向，因此報表算的是依案由大類調整後的差異，並以 Benjamini–Hochberg
控制多重比較的偽發現率。

> ⚠ 使用 `grant_ratio`（獲償比例）前務必先看 `claimed_source`。
> 值為「主文回推」的列，請求金額是用判准金額代入的，比例恆為 100%，
> 拿去平均是循環論證。程式已強制只在「直接抽取」時計算比例。

## 注意事項

- **日期參數會先驗證再開瀏覽器**：格式必須是西元 `YYYY/MM/DD` 或 `YYYY-MM-DD`，且必須是真實存在的日期；起日晚於迄日、誤填民國年（`114/03/01`）、用點分隔（`2024.03.01`）都會在參數階段就被擋下來。這一層存在的理由是：無法解析的日期若被静默忽略，查詢會變成「沒有日期範圍」——你以為在抓一週，實際在抓全部，而且沒有任何錯誤訊息
- 「114 年」有兩種意思，兩者是不同維度：裁判日期民國 114 年用 `--start-date 2025/01/01
  --end-date 2025/12/31`；案號年度 114 年度用 `--case-year-start 114 --case-year-end 114`
- 單一法院＋單一類別的資料量仍然很大（臺灣臺北地方法院、裁判日期 114 年、民事約 9.2 萬筆），
  且其中絕大多數是裁定（司票、補字等）；只要判決時務必加 `--judgment-type 判決`，
  過濾發生在下載之前，可省下大量請求時間
- **速率控制**：請求間隔為 `--delay`（預設 1.0 秒，清單翻頁 0.6 倍）並加上 ±30% 隨機抖動。
  遇到錯誤頁、逾時或 WebDriver 異常時，**後續所有請求**的間隔會加倍（上限 8 倍）；
  連續 5 次失敗則暫停 5 分鐘；連續成功 10 次後倍率減半，逐步回到正常速度。
  批次爬取全程共用同一組節奏狀態，結束時會在日誌印出實際速率（次/分）與降速次數。
  跑整年這類長任務建議 `--delay 1.5` 或 `2`——放慢反而比較快跑完，因為不會反覆被擋
- 短時間內查詢過於密集時，伺服器會讓結果集（網址上的 `q=` hash）失效，清單頁變成
  「查詢設定錯誤」。程式會自動重載並重送查詢復原；若仍失敗，該區段會記為 `page_errors`，
  並在批次結束時列出「需要重跑的日期區間」—— 看到這段訊息就以相同條件重跑那幾天
- 網站回報的筆數本身有雜訊：摘要頁的「查詢結果 N」、清單頁的「共 N 筆」與實際不重複
  筆數三者不一致（清單會出現重複列），且裁判書會持續補登，同一查詢隔幾天再跑總數會變動
- 爬蟲速度受限於司法院伺服器，尖峰時段可能較慢或出現 timeout，程式會自動重試
- 首次執行時 `webdriver-manager` 會自動下載對應版本的 chromedriver
- **日誌位置**：`crawler.py`、`crawl_batched.py`、`crawl_monthly.py`、`pipeline.py` 的日誌都寫入
  `crawler.log`（同時輸出到主控台）；單獨執行 `html_parser.py` 或 `export_excel.py` 時寫入 `parser.log`。
  批次結束時的「需要重跑的日期區間」與「分群桶達上限」警告都在 `crawler.log` 裡

## 進階查詢欄位支援對照

司法院[進階查詢頁](https://judgment.judicial.gov.tw/FJUD/Default_AD.aspx)共有 8 組條件，目前的支援情況：

| 網頁欄位 | 支援 | 對應參數／說明 |
|---|---|---|
| 裁判法院 | ✅ 部分 | `--court`，**只能單選**（網頁可複選多個法院） |
| 案件類別（憲法／民事／刑事／行政／懲戒） | ✅ | `--case-type`，可逗號分隔多選 |
| 裁判期間 | ✅ | `--start-date` / `--end-date`，並自動切分突破 500 筆上限 |
| 全文內容 | ✅ | 關鍵字（位置參數） |
| 裁判字號（年度＋字別＋案號區間） | ❌ | 網頁要求三者同時填寫，目前未支援 |
| 裁判案由 | ❌ | 未支援 |
| 裁判主文 | ❌ | 未支援 |
| 裁判大小（K） | ❌ | 未支援 |

網頁上**沒有**、但本系統另外提供的篩選：

| 功能 | 做法 |
|---|---|
| 裁判種類（判決／裁定） | `--judgment-type`：全文粗篩＋清單頁依裁判字號精篩，下載前就濾掉 |
| 案號年度範圍 | `--case-year-start` / `--case-year-end`：走結果頁的案號年度分群，不是表單欄位 |
| 依法官篩選 | 無專屬參數。可把法官姓名當關鍵字爬取（全文必含姓名，實測漏抓 0），再以資料庫 `judges` 欄位精篩 |

## 已知限制

- **法院只能單選**，要多個法院需分次執行；表單的裁判字號、案由、主文、裁判大小四組條件尚未支援
- **沒有依法官篩選的匯出參數**：`export_excel.py` 無 `judges` 條件，目前需直接查詢資料庫
- **資料標籤是單值的**：同一份裁判書只屬於第一次抓到它的批次。以標籤匯出（`pipeline.py`、
  `export_excel.py -k`）會漏掉先前被其他批次抓過的同條件資料；`crawl_monthly.py` 依條件匯出不受影響
- **網站本身的筆數有雜訊**：清單會出現重複列、裁判書持續補登，單次爬取可能有少量缺漏。
  以相同條件重跑同一區間可補回（已下載的會自動跳過），但目前**沒有逐段對帳**機制可自動證明完整
- **分群桶本身達 500 筆時**，該桶較舊的部分取不到，重跑也無法補救（會在日誌列出；只收判決時因有全文粗篩，實務上罕見）
- **`crawl_batched.py` 與 `pipeline.py` 沒有斷點續爬**，中斷後重跑同一區間需重新翻頁；長時間任務請用 `crawl_monthly.py`
- **全文粗篩「漏抓 0」是抽樣驗證**（單日 60 筆判決逐筆比對），不是全量證明
- **解析品質**（全庫 12,416 筆統計；括號內為臺北地院 2025 年判決 10,552 筆）：
  - `結論` **100% 空白**，原因是結構性的而非抽取失敗：全庫只有 238 筆（1.9%）本文
    出現「據上論斷」，且**全部夾在段落中間**（「據上論斷，本件原告之訴為無理由…」），
    不是獨立的段落標題，`_SECTION_TITLES` 的完整字串比對因此不會將它切成段落。
    內容沒有遺失，95.8% 已落在 `reasons` 或 `facts_and_reasons`。
    欄位保留（schema 與刑事判決共用）
  - `other_parties` **100% 空白**是因為 `html_parser.py` 目前固定寫入空字串，
    從未實作抽取；未匯出到 Excel
  - `適用法條` 的**資料庫原始欄**空白 65.7%（65.2%），且有值者 85.7% 被截斷。
    **Excel 匯出的「適用法條」欄已改用 `applicable_laws_json`**，空白率 0.9%（0.2%）
  - `原告／聲請人`、`被告／相對人` 分別空白 8.7%（5.1%）、24.9%（23.2%），
    但**欄位空白不等於沒抓到當事人**：上訴審記為上訴人／被上訴人、非訟記為聲請人／
    相對人、除權判決本來就沒有對造。以記錄全部角色的 `party_roles` 計，
    全庫僅 **5 筆（0.04%）** 完全沒有當事人資料
  - `法官` 空白 0.5%（0.45%）、`主文` 空白 0.1%（**0 筆**）——13 筆空主文全部是他院
    「補字」裁定這類本來就沒有主文段落的文書
  - 改善方式是修改 `html_parser.py` 後執行 `python html_parser.py --reparse`，
    會從 `html_cache/` 重建，**不需重爬**
- **`export_excel.py` 的排序固定為解析時間**；依裁判日期排序請用 `crawl_monthly.py --export-only`
